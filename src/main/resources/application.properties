spring.application.name=StudentPortal

# DataSource settings: set here your own configurations for the database connection
spring.datasource.url=jdbc:mysql://localhost:3306/StudentPortal?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=qwerty@123

# Specify the JDBC driver for MySQL
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate settings
# Show or not log for each sql query
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Hibernate ddl auto (create, create-drop, update): only for testing purpose, not for production
spring.jpa.hibernate.ddl-auto=create

